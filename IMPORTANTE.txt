Para que este programa funcione debe de descargar la aplicación de nodejs por que si no la tiene no funcionara

Comandos para que funcione:
Dirijirse a la carpeta donde esta almacenado el proyecto
Ingresar el comando 

$ npm install

Eso instalara los modulos necesarios que estan dentro de las dependencias de package.json

$ node server.js

Correra un servidor para ejecutar el proyecto

Abrir index.html en el navegador
